# The default configuration file. The options defined here are overriden by
# options in 'config.local'.
# The ${src_dir} value is determined automatically and contains the name of
# the directory with the source files.

install_dir = ${src_dir}
tasks_dir = ${install_dir}/examples/tasks
rootfs_dir = ${install_dir}/root
home_dir = ${install_dir}/home
meta_dir = ${install_dir}/meta
run_dir = ${install_dir}/run
db_path = ${install_dir}/users
