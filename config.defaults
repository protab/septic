# The default configuration file. The options defined here are overriden by
# options in 'config.local'.
# The ${cwd} value is predefined and means the current working directory.

install_dir = ${cwd}
rootfs_dir = ${install_dir}/root
homefs_dir = ${install_dir}/home
